# Requires: Python 3.10 or newer
# Install: pip install -r requirements.txt
#
# GPU install:
#   The plain `pip install torch` pulls the CPU-only wheel on Linux. For CUDA,
#   install torch first from the PyTorch wheel index, then run this file:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#     pip install -r requirements.txt
#   Substitute cu118 / cu124 / etc. as appropriate for your CUDA toolkit.

# CORE DEEP LEARNING
torch>=2.2.0
torchvision>=0.17.0

# GRADCAM
grad-cam>=1.4.8  # pytorch-grad-cam, works plug-and-play with ResNet + ViT

# DATA PROCESSING
Pillow>=10.0.0
imagehash>=4.3.1  # perceptual hashing for deduplication
pandas>=2.0.0
numpy>=1.24.0

# EVALUATION METRICS
scikit-learn>=1.3.0  # classification_report, confusion_matrix, stratified splits
scipy>=1.11.0        # statistical tests (Welch's t-test) in analysis

# VISUALISATION
matplotlib>=3.7.0
seaborn>=0.12.0

# EXPERIMENT TRACKING
wandb>=0.15.0  # Weights & Biases 

# UTILITIES
pyyaml>=6.0      # loading base_config.yaml
tqdm>=4.65.0     # progress bars
kaggle>=1.6.0    # dataset download via Kaggle API